<%= form_for(:dataset) do |f| %>

 <div class="field" align="center">
    <%= f.file_field :file %>
  </div>
  <div class="actions">
    <%= f.submit 'Save file', :class => 'btn btn-success' %>
  </div>
<% end %>
